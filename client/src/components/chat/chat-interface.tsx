export default function ChatInterface({ minimized = false }) {
  // N8N chat widget is loaded globally via script tag in index.html
  // This component serves as a placeholder where chat was previously displayed
  return null;
}